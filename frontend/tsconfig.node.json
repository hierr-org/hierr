{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"], // usually no DOM for node-side files
    "types": ["node"],
    "composite": true // useful if you actually use project references
  },
  "include": ["vite.config.ts", "scripts/**/*.ts"],
  "exclude": ["node_modules"]
}
